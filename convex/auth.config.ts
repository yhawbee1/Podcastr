//https://willing-cicada-7.clerk.accounts.dev

const authConfig = {
    providers: [
        {
            domain: "https://willing-cicada-7.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;